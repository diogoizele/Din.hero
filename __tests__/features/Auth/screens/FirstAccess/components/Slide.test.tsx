// import React from 'react';
// import { render } from '@testing-library/react-native';
// import { useSharedValue } from 'react-native-reanimated';

// import { Slide } from '@features/Auth/screens/FirstAccess/components/Slide';
// import { SLIDES } from '@features/Auth/screens/FirstAccess/constants/slides';

// jest.mock('@react-native-async-storage/async-storage', () => ({
//   setItem: jest.fn(),
//   getItem: jest.fn(),
//   removeItem: jest.fn(),
//   clear: jest.fn(),
// }));

//  TODO: Uncomment when migrate all new theme and remove react-native-ui-lib
describe('Slide', () => {
  it('remove this', () => {});
  //   const scrollX = { value: 0 } as ReturnType<typeof useSharedValue<number>>;
  //   it('renders headline text for the given slide', () => {
  //     const { getByText } = render(
  //       <Slide slide={SLIDES[0]} index={0} scrollX={scrollX} />,
  //     );
  //     expect(getByText(SLIDES[0].headline)).toBeTruthy();
  //   });
  //   it('renders subline text for the given slide', () => {
  //     const { getByText } = render(
  //       <Slide slide={SLIDES[0]} index={0} scrollX={scrollX} />,
  //     );
  //     expect(getByText(SLIDES[0].subline)).toBeTruthy();
  //   });
  //   it('renders the correct content for each slide in SLIDES', () => {
  //     SLIDES.forEach((slide, i) => {
  //       const { getByText, unmount } = render(
  //         <Slide slide={slide} index={i} scrollX={scrollX} />,
  //       );
  //       expect(getByText(slide.headline)).toBeTruthy();
  //       expect(getByText(slide.subline)).toBeTruthy();
  //       unmount();
  //     });
  //   });
});
